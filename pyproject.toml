[project]
name = "satudata-mcp"
version = "0.1.0"
description = "MCP server for Satu Data Garut open data portal"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
    "httpx>=0.27.0",
]

[project.scripts]
satudata-mcp = "satudata_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/satudata_mcp"]
